# fastfft — швидкий FFT-модуль для MicroPython (RP2040)

`fastfft` — це користувацький C-модуль для MicroPython, призначений для
швидкого обчислення **real FFT (rFFT)** та **енергії спектра** на
мікроконтролерах **RP2040** (Raspberry Pi Pico та сумісні).

Модуль оптимізований для:
- роботи без heap-алокацій під час виклику,
- мінімального Python-overhead,
- стабільної та передбачуваної роботи в реальному часі.

---

## 📦 Особливості

- FFT розміром **512 відліків**
- Вхідні дані: `int16` (`array('h')`)
- Вихідні дані: `int32` energy spectrum (`memoryview('i')`)
- FFT реалізована на базі **kissFFT (float)**
- Обчислення енергії виконується **в C-коді**
- Повернення результату **без копіювання** (zero-copy)
- Підходить для аудіо, сенсорів, спектроаналізу

---

## ⚙️ Вимоги

- MicroPython з підтримкою user C-modules
- Платформа: **RP2040**
- Частота CPU: 133 МГц (рекомендовано)

---

## 🧠 Теоретична модель

Для вхідного сигналу:  
x[n] = A · sin(2πkn / N)  
де `N = 512`, модуль обчислює:  
Energy[k] = |FFT[k]|² / (N²)  
Для чистої синусоїди, частота якої точно потрапляє в FFT-бін:  
PeakEnergy ≈ A² / 4  

---

## 📌 API

### `fastfft.rfft(input)`

Обчислює real FFT і повертає **енергію спектра**.

#### Параметри
- `input` — `array('h')` довжиною **512**
  - значення `int16`
  - рекомендований діапазон: `±10000`

#### Повертає
- `memoryview('i')` довжиною **256**
  - `int32`
  - енергія спектра (`|FFT|²`, нормалізована)

#### Зауваження
- DC-компонента (`bin 0`) примусово дорівнює `0`
- Nyquist-бін (`256`) не повертається

---

## 🧪 Приклад використання

### Генерація однієї гармоніки та FFT

```python
from array import array
import math
import fastfft

FFT_SIZE = 512
fs = 8000          # частота дискретизації
k = 32             # номер FFT-біна
f = fs * k / FFT_SIZE
amp = 10_000

buf = array('h', [0]*FFT_SIZE)

for n in range(FFT_SIZE):
    buf[n] = int(amp * math.sin(2*math.pi*f*n/fs))

spec = fastfft.rfft(buf)

peak_bin = max(range(len(spec)), key=lambda i: spec[i])
print("Peak bin:", peak_bin)
print("Peak energy:", spec[peak_bin])
```

Очікувано:  
```python
Peak bin: 32
Peak energy ≈ 25_000_000
```
## 📊 Вхідні дані та масштабування
#### Тип вхідних даних  
```python
array('h')  # int16
```
#### Рекомендований діапазон  
```python
±10000  (оптимально)
±15000  (верхня безпечна межа)
```
Використання повного діапазону int16 (±32767) не рекомендується  
через:
- можливий кліпінг джерела сигналу,
- втрату точності float-обчислень.

---  

## ⏱ Продуктивність (RP2040 @133 МГц)

Повний виклик rfft() ~12 ms  
Як вимірювалось: 
```python
from array import array
import fastfft
import time

buf = array('h', [0] * 512)

# прогрів (важливо!)
for _ in range(5):
    fastfft.rfft(buf)

t0 = time.ticks_us()
s = fastfft.rfft(buf)
t1 = time.ticks_us()

dt = time.ticks_diff(t1, t0)
print("FFT time:", dt, "us")
```
---  

## ⚗️ Деякі перевірки та приклади використання
#### ✅ Перевірка на збереження енергії сигналу після перетворення (теорема Ляпунова-Парсеваля)
```python
from array import array
import fastfft
import random


FFT_SIZE = 512
amp = 10_000        # пік сигналу (без clipping)

buf = array('h', [0]*FFT_SIZE)

# тестовий сигнал білий шум
for i in range(FFT_SIZE):
    buf[i] = random.randint(-amp, amp)

# енергія у часі
Et = sum(x*x for x in buf)

spec = fastfft.rfft(buf)

# енергія у частоті
Ef = 2 * sum(spec[1:]) * FFT_SIZE

print(f'Повинні бути ~однакові: {Et} ~= {Ef}') # повинні бути ~однакові
```
#### :seedling: обчислення рівня сигналу в dBFS (дБ повної шкали) для смуги частот  
```python
from array import array
import math
import fastfft
import random


FFT_SIZE = 512
amp = 10_000        # пік сигналу (без clipping)

buf = array('h', [0]*FFT_SIZE)

# тестовий сигнал білий шум
for i in range(FFT_SIZE):
    buf[i] = random.randint(-amp, amp)

spec = fastfft.rfft(buf)

# Опорна потужність повномасштабного синуса (Standard AES17 Reference)
FS_RMS2 = (32767 / math.sqrt(2))**2

# значення dBFS для смуги частот
def band_dbfs(spec, i, j):
    
    e = 0
    for k in range(i, j):
        e += spec[k]
    
    if e <= 0:
        return -120
    
    return 10 * math.log10((2 * e) / FS_RMS2)

pin_1, pin_2 = 4, 50 # значення діапазону пінів для розрахунку
dbfs_for_band = band_dbfs(spec, pin_1, pin_2)
print(dbfs_for_band)
```

---  

## 🔒 Гарантії безпеки
- Всі буфери — статичні
- Відсутні malloc/free під час виконання
- Захист від переповнення int32
- Перевірка типів і розміру вхідного масиву

---  

## 📄 Credits and License
- **fastfft module**: Licensed under MIT.
- **kissFFT**: This project uses the [kissFFT](https://github.com/mborgerding/kissfft) library by Mark Borgerding, licensed under the **BSD 3-Clause License**.
---  


